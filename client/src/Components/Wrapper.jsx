const { Container } = require("@mui/material");

const Wrapper = () => {
  return (
    <Container
      sx={{ width: "100%", display: "flex", justifyContent: "center" }}
    ></Container>
  );
};

export { Wrapper };
